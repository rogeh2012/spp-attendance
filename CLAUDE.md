# CLAUDE.md — SPP Attendance

Project brief for Claude Code. Read this fully before touching anything.

## What this is

QR-code attendance for the **School of Praise & Prayer** (مدرسة التسبيح والصلاة), a 5-week church program in Alexandria, Egypt.

- Runs **Fri 18 Sept – Fri 16 Oct 2026** — 8 days, 11 sessions. Fridays have **1 session**, Saturdays have **2 sessions**.
  - Sept: **Fri 18, Sat 19, Fri 25, Sat 26** · Oct: **Fri 2, Fri 9, Sat 10, Fri 16**.
  - There is **no** session on Sat 3 Oct or Sat 17 Oct. The app doesn't know the calendar — it just
    offers Session 1 on Fridays and 1+2 otherwise — so on a non-school day nobody scans and nothing is recorded.
- **80–100 students**, each with a personal QR code (printed card and/or PNG sent on WhatsApp).
- **One admin** signs in on a phone, picks the session, opens the camera, scans each student.
- Every scan is stored with the student, the date, the session number, and the exact time in **Cairo time**.
- Attendance is viewed in the app and exported to **Excel/CSV**.
- Live at **https://attendance.rajrooj.com** (Hostinger Node.js Web Apps, Business plan).
- Data is only needed for these 5 weeks; at the end, export to Excel and pause/delete the Supabase project.

The app is already built and compiles clean. Your job is setup, deployment, and small changes — not a rewrite.

## Stack

- **Next.js 16** (App Router, TypeScript), every page is a client component. No API routes, no middleware, no server code.
- **Supabase**: Auth (email + password, signups disabled) and Postgres (RLS on, `record_scan()` RPC).
- `html5-qrcode` for camera scanning, `qrcode` for QR rendering (canvas), `xlsx` (SheetJS) for export. Plain CSS in `app/globals.css`, no Tailwind.
- The app is fully static-exportable: uncomment `output: "export"` in `next.config.ts` if it ever needs to run on PHP-only hosting.

## Repo map

```
app/
  layout.tsx            font link, metadata, viewport
  globals.css           design tokens + all styles
  login/page.tsx        email/password sign-in
  page.tsx              Scan — session picker, camera, result flash, running count, recent list
  attendance/page.tsx   per-day list, remove a scan, Export Excel/CSV (all days + Summary sheet)
  students/page.tsx     list/search/add/remove, per-student QR sheet with Download PNG
  print/page.tsx        all cards in a grid → browser Print → Save as PDF
components/
  AuthGate.tsx          redirects to /login without a session
  Shell.tsx             header + tab nav + sign out, wraps AuthGate
  ManualEntry.tsx       search a student by name → mark present (no QR needed), via record_scan()
  Scanner.tsx           html5-qrcode wrapper (client-only, loaded with next/dynamic ssr:false)
  QrCard.tsx            QR card component + downloadPng() (900×1100 canvas PNG for WhatsApp)
lib/
  supabase.ts           browser client singleton + row types
  time.ts               Africa/Cairo helpers: cairoToday, cairoTime, cairoDateTime, sessionsForToday
supabase/schema.sql     tables, token generator, record_scan RPC, RLS, attendance_cairo view
scripts/import-students.mjs   CSV → students (needs service role key, run locally only)
.env.example            NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY
```

## Data model (do not change without a reason)

```
students   id, name, phone, group_name, qr_token (unique, e.g. SPP-K7M3Q9XZ), created_at
attendance id, student_id → students, session_date (date, Cairo), session_no (1|2),
           scanned_at (timestamptz, UTC), scanned_by (auth.users.id)
           UNIQUE (student_id, session_date, session_no)
```

- `record_scan(p_token, p_session_date, p_session_no)` → `{status: recorded|duplicate|unknown, student_name, scanned_at}`. Security definer, requires `auth.uid()`.
- The QR contains **only the token**, never the name or phone.
- Tokens use the alphabet `ABCDEFGHJKLMNPQRSTUVWXYZ23456789` (no 0/O/1/I) and are generated by the database default — never generate them in JS.

## Decisions that must survive any edit

1. **Time**: store UTC, display with `Intl.DateTimeFormat` + `timeZone: "Africa/Cairo"`. Never use a fixed +2/+3 offset — Egypt observes DST, and it ends in late October, just after the school finishes; a hard-coded offset would be wrong the moment that changes. The session date is also computed in Cairo (`cairoToday()`), not from the phone's clock.
2. **One scan per student per session** is enforced by the database unique constraint. The UI additionally ignores the same token re-read within 3 seconds. Don't move the duplicate check to the client only.
3. **Camera needs HTTPS.** Any deploy or preview URL must be https or the scanner silently fails.
4. **Arabic names**: QR cards and PNGs are rendered with browser canvas/HTML on purpose — server-side PDF libraries (pdfkit, reportlab without reshaper) break Arabic letter joining. Don't "improve" this by generating PDFs in Node.
5. **Anonymous users get nothing** (RLS + revokes). Never expose the service role key to the browser; it's only used by `scripts/import-students.mjs` and admin scripts run locally.
6. Keep the UI small. English only. Three tabs. No new dependencies unless asked.

## Setup — do these in order

Ask the user for anything marked `<...>`; never invent values.

### 1. Local build

```bash
npm install
cp .env.example .env.local     # fill in from Supabase → Project Settings → API
npm run build                  # must pass before anything else
npm run dev                    # http://localhost:3000 — login page should render
```

### 2. Supabase project

The user creates a **new** project (free tier) under the **rogeh2012@gmail.com** account, which owns all of this app's infrastructure. Never reuse an existing project from another account — this database holds only the school.

Then, with the Supabase MCP (or by pasting into the SQL Editor), run `supabase/schema.sql` once. Verify:

```sql
select count(*) from public.students;                 -- 0
select proname from pg_proc where proname = 'record_scan';
```

Dashboard settings the user must click (no API for these on free tier):
- Authentication → Sign In / Providers → **Allow new users to sign up: OFF**
- Authentication → Providers → Email: enabled, "Confirm email" can stay on (admin is auto-confirmed below)

### 3. Admin user

Only one admin. Create with the service role key (never commit it):

```bash
SUPABASE_URL=<url> SUPABASE_SERVICE_ROLE_KEY=<key> node -e '
import("@supabase/supabase-js").then(async ({createClient}) => {
  const sb = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY);
  const { data, error } = await sb.auth.admin.createUser({ email: "<admin email>", password: "<strong password>", email_confirm: true });
  console.log(error ?? data.user.id);
})'
```

### 4. Students

The user has a text list: one student per line, **name and phone**. Convert it to `students.csv` with header `name,phone` (leave `group` out). Watch for: Arabic names with commas, phone numbers with spaces/`+20`, duplicate names (keep both — the token is the identity). Then:

```bash
SUPABASE_URL=<url> SUPABASE_SERVICE_ROLE_KEY=<key> node scripts/import-students.mjs students.csv
```

It prints `token \t name` for every inserted student. Confirm the count matches the list. Do not commit `students.csv` or `students.txt`.

### 5. QR codes for distribution

Two built-in ways, both from the deployed or local app after signing in:
- **Students → QR → Download PNG** — one image per student, ready for WhatsApp.
- **Students → Print all cards → Print → Save as PDF** — 3 cards per row on A4.

If the user wants all PNGs in one go without clicking, write a small Playwright script that signs in, opens `/print`, and screenshots each `.card` element to `qr/<token>.png`. Use Playwright, not a Node QR library, so Arabic renders correctly.

### 6. GitHub

```bash
git init && git add -A && git commit -m "SPP attendance"
gh repo create spp-attendance --private --source=. --push
```

`.gitignore` already excludes `.env*`, `node_modules`, `.next`, `out`.

### 7. Hostinger deploy (user does this in hPanel — give them the exact steps)

1. hPanel → Websites → **Add Website** → **Node.js Apps** → Import Git Repository → authorize GitHub → pick `spp-attendance`, branch `main`.
2. Build settings: install `npm ci`, build `npm run build`, start `npm run start -- -p $PORT`, Node.js **22**.
3. Environment variables: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`.
4. Domain: attach **attendance.rajrooj.com**. If rajrooj.com's DNS is managed at Hostinger, the record is added for you. If it's on Cloudflare, add the record Hostinger shows (A or CNAME) with the proxy **off** (DNS only) until SSL is issued; it can be turned on afterwards.
5. Wait for SSL, open https://attendance.rajrooj.com/login on a phone.

Every later `git push` to `main` redeploys.

### 8. Test on a real phone before 18 Sept

- Open the URL in Safari (iPhone) or Chrome (Android) **directly**, not from inside WhatsApp's in-app browser.
- Sign in → Session 1 → Open camera → allow permission → scan a printed card and a PNG on another phone's screen.
- Expect: green flash with the name, count goes up; scan again → amber "Already recorded"; scan a random QR → red "Unknown QR code".
- Attendance tab → the scan is listed with the Cairo time → Export Excel opens in Excel with two sheets.
- Sign out → `/` redirects to `/login`.

## Runbook during the school

- Friday: the picker shows only Session 1. Saturday: Session 1 and Session 2 — pick before opening the camera.
- Wrong scan: Attendance tab → × next to the row.
- **Forgot their QR code**: Scan tab → **Add manually** → type part of the name → Add. It goes through the same
  `record_scan()` as the camera, so it is recorded identically — there is no "manual" marking, present is present.
  Arabic search is forgiving: typing `احمد` finds `أحمد`, `فاطمه` finds `فاطمة`.
- **Missed someone on a day that already passed**: Attendance tab → pick the date → **Add manually** under that
  session's heading. The day comes from the date picker, the session from the block it sits under.
- New student mid-school: Students → Add → QR → Download PNG.
- Lost card: Students → search → QR (show the phone screen, or resend the PNG).
- Free-tier Supabase pauses after 7 idle days. Fri→Sat→Fri is 6 days so it's fine; if a week is skipped, restore the project from the dashboard before the next session.

## After 16 Oct

Attendance → Export Excel (keep the file), then pause or delete the Supabase project. The Hostinger app can be deleted.

## Things not to do

- Don't add Google Sheets sync, multi-admin roles, student self-check-in, or notifications — out of scope.
- Don't switch to `next/font/google` (build environments without internet break); the font is loaded by `<link>` in `layout.tsx` with a system fallback.
- Don't put any secret in `NEXT_PUBLIC_*`.
- Don't run `scripts/import-students.mjs` twice on the same list — it inserts, it doesn't upsert.
